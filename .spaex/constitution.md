<!-- spaex-composed:source_hash="2e49ffb8cee6f28e1d46c986ab5bfb613b0ad83128df98354d93e5f392513263" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- Maintain test code in files separate from production code, for unit, integration, contract, and end-to-end tests alike, with test bodies and test-only helpers living in dedicated test files; in Rust, put focused unit-test implementations in a separate test file, use integration tests under `tests/` to exercise the public API as an external consumer would, and use documentation tests for public examples that should compile and remain correct, following the repository's convention for test-file placement and naming. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- For any existing Git repository, author all changes — source code, tests, documentation, configuration, and generated files that are part of the change — in a dedicated repository-local worktree (one registered via `git worktree list`, not a copied directory or unrelated clone) on a topic branch; before the first edit, verify the repository root, active branch, and registered worktrees, and create a suitable worktree first if none exists, so each task stays isolated and independently reviewable, testable, and removable. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat 500 LoC as the upper boundary for hand-maintained source files: a file clearly over 500 LoC must be split, or have an explicit, documented reason to remain large together with a concrete plan for a later split; the boundary is a maintainability constraint, not a target. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_

## MUST_NOT

- Do not add `.clone()` merely to silence the borrow checker. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not discard the original error or replace it with an uninformative string when adding context to it. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not encode a closed set of states or an explicitly optional value as sentinel values or loosely related booleans. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not perform blocking file, process, or CPU-heavy work directly on an async executor thread, and do not create a new runtime inside an already-running async context. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not use `unwrap()` or `expect()` on data, I/O, configuration, or network input. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not introduce a new crate, dependency, or async strategy without a concrete problem that the existing code and its already-installed dependencies cannot solve. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not chase a coverage percentage at the expense of meaningful assertions. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Do not depend on test order, shared mutable state, wall-clock timing, network services, or a developer-specific filesystem in tests. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Do not use arbitrary sleeps to wait for a task in tests. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Do not author changes in the primary checkout; reserve it for inspection and worktree management (read-only inspection is fine). _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not create folders only for superficial symmetry, such as putting every small class, interface, or helper in its own folder. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not place inline test modules or test functions in production files beyond a bare language-required declaration. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not present a green-looking diff as verified without having actually run the applicable checks. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not use comments to narrate code that could be made clear through structure and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- LLM agents must not leave references to themselves, their model, runtime, role, prompts, or participation in project artifacts — including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, metadata, and other repository content. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Never add artificial wrappers, blank lines, or extra files only to satisfy a line-count threshold, and do not pad files up to the 500 LoC boundary; a short but tangled module is not healthy, and a long but cohesive module is not automatically wrong. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not abstract two things merely because they look similar, or introduce a speculative abstraction solely because a change might be reusable; first establish that they share the same behavior, ownership, change reason, and invariants — a small amount of local duplication is preferable to a premature generic abstraction that couples unrelated callers. _[from `com.github.haexmas.atoms.general-coding/general-coding`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not add boilerplate nobody asked for. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not cut corners on understanding the problem, input validation at trust boundaries, error handling that prevents data loss, security, accessibility, the calibration real hardware needs, or anything explicitly requested by the task. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD

- Keep async code non-blocking; use the runtime's blocking facility or a dedicated worker for blocking work where appropriate. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Keep unsafe code isolated and justified: prefer a safe abstraction around a small unsafe block, document the safety argument, and add a test that would expose a violation of the assumed invariant. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Prefer iterators and combinators when they make the transformation clearer, but use an ordinary loop when it communicates control flow or error handling better; idiomatic syntax is not a reason to make code harder to read. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Prefer types that make invalid states unrepresentable: use enums for a closed set of states and `Option<T>` for an explicitly optional value. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Respect ownership instead of fighting it: borrow when the callee does not need ownership, and return owned data when the result must outlive the input; a `.clone()` is fine when it expresses a real ownership boundary or is demonstrably the clearest and sufficiently cheap choice. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Use `Result<T, E>` for recoverable failures, propagate errors with `?`, and add context at a boundary where it helps the caller understand what failed. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Before handing off a change, run the project's formatter, linter, and relevant tests — for example `cargo fmt --check`, `cargo check`, `cargo clippy`, and `cargo test` for the complete package or workspace, covering unit, integration, and library documentation tests by default — when the project supports those commands, following project-specific feature, target, test-runner, or command-variant requirements; report what was run and any checks that were unavailable or skipped. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`, `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep public APIs and module interfaces narrow and intentional: hide implementation details behind the smallest stable API callers need, avoid re-exporting everything or creating pass-through modules that add no boundary, and avoid exposing implementation details solely to work around a local borrow or lifetime problem; document public items when the crate's lint policy requires it. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Choose the smallest correct implementation that fits existing conventions — crate conventions in Rust — since the shortest working diff wins, but only once you understand the problem; the smallest change in the wrong place isn't lazy, it's a second bug. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Cover boundaries and failure behavior deliberately: empty and malformed input, relevant limits, authorization decisions, persistence failures, and cancellation or concurrency behavior where applicable. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- For async code, use the async test support of the runtime already used by the project; test cancellation, timeout, and ordering assumptions when they affect correctness. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Keep test helpers small and local to the tests that own them; a shared fixture is justified when it expresses a stable domain setup, not merely to remove a few repeated lines. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Keep tests deterministic and isolated; inject clocks, ports, and external clients where the behavior needs control. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Prefer explicit assertions with useful failure context; avoid weakening a production invariant merely to make a test convenient, and make the fixture satisfy the same contract as a real caller. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Treat a test suite as production code: keep it readable, fast enough to run often, and trustworthy when it passes. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Test observable behavior and contracts — including important failure paths — rather than mirroring private implementation details; a test should fail when the promised behavior regresses, and is useful only when it can fail for a real regression. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Follow a repository's already-documented structure, naming scheme, or size convention unless the task explicitly changes it; this atom supplies defaults, not a reason to reformat or reorganize an entire codebase. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Give a file one primary reason to change; split a file when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate, but not solely to make it shorter. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Give each function one coherent responsibility at one level of abstraction; extract a helper when it gives a meaningful name to a concept, makes a branch independently testable, or removes repeated logic, not merely because the function crossed an arbitrary line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Have comments explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies, and update them when the behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep dependencies directed and understandable: avoid circular imports, hidden global state, and a domain layer that knows about infrastructure details; put I/O, framework adapters, and process wiring at the edges when the architecture calls for such a separation. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each change self-contained and easy to review: production code, relevant tests, documentation, and configuration belong together when they describe the same behavior; split independent refactors and unrelated cleanups into separate changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep related tests close to the behavior they verify, per the language and repository convention; a module split is not complete if its tests, documentation, fixtures, and call sites are left misleadingly behind. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep side effects at visible boundaries; pure calculations and decisions should be easy to test without a database, network, filesystem, clock, or global mutable state. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Name code after its domain meaning and behavior; a clear name is better than a comment that explains an otherwise opaque `data`, `helper`, or `process` function. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Organize code around cohesive domain or feature boundaries where the codebase supports it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a change that clearly improves maintainability and correctness over theoretical perfection; when a design is uncertain, make the smallest reversible change that provides evidence about the next decision. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer early returns or named helpers when they make the main path easier to follow, and reduce nesting, without scattering one simple operation across many microscopic functions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Separate broad formatting or mechanical refactors from behavior changes when possible; mixed diffs make review, debugging, rollback, and blame less reliable. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a change of about 100 net lines as a useful review-size warning, and a change approaching 1000 lines as a strong prompt to split the work or agree on the larger change in advance; generated code, mechanical renames, and required migrations are legitimate exceptions when identified as such. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a function or method beyond roughly 40–60 lines, or nesting deeper than roughly three levels, as a prompt for deliberate review — is it still one coherent unit, or are there distinct responsibilities to separate — as heuristics for review, not mandatory limits. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Add or update tests when behavior changes: non-trivial logic should leave one runnable check behind, the smallest thing that fails if the logic breaks, written with whatever test framework and fixtures the project has already adopted; a project with no such framework yet gets a framework-free assert-based demo or one small test file instead, and trivial one-liners need no test. _[from `com.github.haexmas.atoms.general-coding/general-coding`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Introduce a concrete, domain-owned abstraction when the code evidence supports it — real duplication, meaningful cohesion, or independent testability — even when the task didn't name the extraction; keep it close to the domain that owns it, as small as possible, and don't let a generic utility module become a dumping ground. _[from `com.github.haexmas.atoms.general-coding/general-coding`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Before writing code, follow the priority ladder: skip it if it isn't needed (YAGNI); otherwise reuse what already exists in the codebase, then the standard library, a native platform feature, an already-installed dependency, a one-line solution, and only then the minimum code that works. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Fix a bug at its root cause, not just the reported symptom: grep every caller of the function you touch and fix the shared function once, rather than patching only the path the ticket names. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Mark a deliberate simplification that cuts a real corner with a known ceiling — a global lock, an O(n²) scan, a naive heuristic — with a `ponytail:` comment naming the ceiling and the upgrade path. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Pick the edge-case-correct option when two stdlib approaches are similarly sized; lazy means less code, not the flimsier algorithm. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer deletion over addition, boring over clever, and the fewest files possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Question complex requests: ask whether something is actually needed or a simpler alternative already covers it. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Run the priority ladder only after understanding the problem, not instead of it: read the task and the code it touches, and trace the real flow end to end before picking a rung. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD_NOT

- Do not enable stricter Clippy lint categories or `-D warnings` beyond the repository's configured lint levels unless the project deliberately requires them. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not treat a universal maximum line count for functions, files, or pull requests as a hard rule; domain complexity, cohesion, control flow, and the number of reasons to change are more meaningful than a line count alone. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_

## MAY

- `unwrap()`/`expect()` may be used when an invariant is local, explicit, and genuinely impossible to violate, provided that invariant is documented at the call site. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- A language-required test-module declaration or registration — such as Rust's `#[cfg(test)] mod tests;` — may remain in the production file, provided the test implementation itself lives in the separate dedicated test file. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Generated files, vendored code, snapshots, and other files not normally hand-edited may exceed the 500 LoC boundary, and should be excluded from size assessment. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
